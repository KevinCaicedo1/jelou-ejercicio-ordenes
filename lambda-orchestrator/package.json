{
  "name": "lambda-orchestrator",
  "version": "1.0.0",
  "description": "Lambda orquestador para creacion y confirmacion de pedidos B2B",
  "main": "src/handlers/orchestrator.js",
  "scripts": {
    "dev": "serverless offline start",
    "deploy": "serverless deploy",
    "test": "jest --runInBand",
    "test:coverage": "jest --coverage",
    "lint": "eslint src/**/*.js"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "dotenv": "^16.0.0",
    "jsonwebtoken": "^9.0.0",
    "zod": "^3.22.0"
  },
  "devDependencies": {
    "jest": "^29.0.0",
    "serverless": "^3.38.0",
    "serverless-dotenv-plugin": "^6.0.0",
    "serverless-offline": "^13.3.0"
  }
}
